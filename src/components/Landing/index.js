import React from 'react';
import Navigation from "../Navigation";

const Landing = () => (
    <div>
        <Navigation />
        <main>
            <div>
                <h1>Landing</h1>
            </div>
        </main>
    </div>
);

export default Landing;